<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>EJS template</h1>

    <!-- locals.var || 'Default' (it will always work doesn't matter the name) if you have a custom params -->
    <!-- <%= locals.text || 'Default' %> -->

    <ul>
        <% users.forEach((user) => { %>
            <li><%= user.id %>, <%= user.fullName.firstName %>, <%= user.email %></li>
        <% });%>
    </ul>
</body>
</html>
