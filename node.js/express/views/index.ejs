<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>EJS template</h1>

    <ul>
        <% users.forEach((user) => { %>
            <li><%= <!-- locals.var || 'Default' (it will always work doesn't matter the name) --> user.id %>, <%= user.fullName.firstName %>, <%= user.email %></li>
        <% });%>
    </ul>
</body>
</html>
